apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wms-app-tls
  namespace: wms
spec:
  secretName: wms-app-tls  # This matches the name in your Gateway spec
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - app.datanetwork.online
